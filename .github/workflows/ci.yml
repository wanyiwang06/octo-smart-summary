name: CI

on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review, converted_to_draft]
    # converted_to_draft kept: creates "skipped" check entries satisfying required-check rules
    branches: [main]
    # paths-ignore intentionally removed — use changes preflight job instead
    # (workflow-level path filters deadlock required status checks for docs-only PRs)
  push:
    branches: [main]
    paths-ignore:
      - "**/*.md"
      - "docs/**"
      - ".github/ISSUE_TEMPLATE/**"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  GO_VERSION: '1.25.x'

jobs:
  changes:
    name: Detect changed paths
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      pull-requests: read
    outputs:
      # code=true when at least one changed file matches the code filter.
      # predicate-quantifier:every + negation patterns (no bare '**'):
      # a file matches 'code' iff ALL negation patterns are satisfied
      # (file is not a doc/template). code=false only when all changed files
      # are docs/templates. Push events default to 'true'.
      code: ${{ github.event_name == 'pull_request' && steps.filter.outputs.code || 'true' }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        if: github.event_name == 'pull_request'
        with:
          persist-credentials: false
      - uses: dorny/paths-filter@6852f92c20ea7fd3b0c25de3b5112db3a98da050 # v3
        id: filter
        if: github.event_name == 'pull_request'
        with:
          # predicate-quantifier: every — a file matches 'code' only when ALL
          # negation patterns are satisfied (file is NOT a doc/template file).
          #
          # Validated by picomatch v4 unit test (same library used internally):
          #   README.md:    !**/*.md=false → every=false → not match → code=false ✅
          #   main.go:      all negations=true → every=true → match → code=true ✅
          #   mixed PR:     main.go matches → code=true (some over changed files) ✅
          #   .golangci.yml: all negations=true → every=true → CI runs ✅
          #   Dockerfile:   all negations=true → CI runs ✅
          #
          # Do NOT add '**': picomatch('**') doesn't match dotfiles (.golangci.yml)
          # causing them to incorrectly skip CI with the 'every' quantifier.
          # Do NOT use 'some' + negation: '**' short-circuits some to true always.
          predicate-quantifier: 'every'
          filters: |
            code:
              - '!**/*.md'
              - '!docs/**'
              - '!.github/ISSUE_TEMPLATE/**'


  build:
    needs: [changes]
    if: |
      (github.event_name != 'pull_request' || !github.event.pull_request.draft) &&
      needs.changes.outputs.code == 'true'
    name: Build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: Build
        run: CGO_ENABLED=0 go build -v ./...

  test:
    if: |
      (github.event_name != 'pull_request' || !github.event.pull_request.draft) &&
      needs.changes.outputs.code == 'true'
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [changes]
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: Test
        # CGO_ENABLED=0: disable CGO to avoid linking libtokenizers.a.
        # HF tokenizers fall back to estimate mode. Tests with //go:build cgo
        # (sqlite-dependent: db, pipeline, worker, api/handler) are skipped.
        # Full coverage with -race runs in the separate test-race job.
        run: CGO_ENABLED=0 go test -shuffle=on -count=1 -timeout 5m ./...

  vet:
    needs: [changes]
    if: |
      (github.event_name != 'pull_request' || !github.event.pull_request.draft) &&
      needs.changes.outputs.code == 'true'
    name: Vet
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: Vet
        run: CGO_ENABLED=0 go vet ./...

  generate:
    needs: [changes]
    if: |
      (github.event_name != 'pull_request' || !github.event.pull_request.draft) &&
      needs.changes.outputs.code == 'true'
    name: Generate (no drift)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version: ${{ env.GO_VERSION }}
      # The fence guard's delimiter and tag-name alphabets are DERIVED from Unicode
      # properties (internal/api/handler/gen_fence_delims.go), because a
      # hand-maintained homoglyph list is a set an attacker picks from the
      # complement. That premise only holds if the checked-in table actually matches
      # what the generator produces today, so drift is a correctness failure and not
      # a formatting nit: a stale table silently narrows the guarded alphabet.
      - name: Regenerate and check for drift
        run: |
          CGO_ENABLED=0 go generate ./...
          if ! git diff --exit-code; then
            echo "::error::Generated files are stale. Run 'go generate ./...' and commit the result."
            exit 1
          fi

  test-race:
    needs: [changes]
    if: |
      (github.event_name != 'pull_request' || !github.event.pull_request.draft) &&
      needs.changes.outputs.code == 'true'
    name: Test (race, cgo)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: Install libtokenizers
        run: |
          curl -fsSL https://github.com/daulet/tokenizers/releases/download/v1.27.0/libtokenizers.linux-amd64.tar.gz | tar xz
          sudo mv libtokenizers.a /usr/local/lib/
      - name: Test with race detector
        run: CGO_ENABLED=1 CGO_LDFLAGS="-L/usr/local/lib" go test -race -shuffle=on -count=1 -timeout 10m ./...

  lint:
    needs: [changes]
    if: |
      (github.event_name != 'pull_request' || !github.event.pull_request.draft) &&
      needs.changes.outputs.code == 'true'
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: golangci-lint
        uses: golangci/golangci-lint-action@1e7e51e771db61008b38414a730f564565cf7c20 # v9.2.0
        with:
          version: v2.12.2
          args: --timeout=5m
